import { DashboardNav } from "../dashboard/DashboardNav"

export const Saved = ()=>{
    return <section>
        <DashboardNav/>
        <h1 className="text-black">Welcome to Our Community</h1>
    </section>
}