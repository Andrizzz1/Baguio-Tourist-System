import { DashboardNav } from "../dashboard/DashboardNav"

export const Saved = ()=>{
    return <section>
        <DashboardNav/>
        <h1 className="text-black">Welcome to Our Saved</h1>
    </section>
}